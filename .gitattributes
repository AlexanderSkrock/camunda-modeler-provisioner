# Disabled eol conversions
* -text

# Use Git LFS for large files
test-resources/**/*.zip filter=lfs diff=lfs merge=lfs -text
test-resources/**/*.tar.gz filter=lfs diff=lfs merge=lfs -text
test-resources/**/*.dmg filter=lfs diff=lfs merge=lfs -text
