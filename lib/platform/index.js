const { currentPlatform, ofProcessPlatform, ofShortName } = require('./platforms');
const Platform = require('./Platform');

module.exports = {
    Platform,
    currentPlatform,
    ofProcessPlatform,
    ofShortName,
};
